server:
  port: 8081
spring:
  application:
    name: etudiant-service
  datasource:
    url: jdbc:postgresql://postgres:5432/etudiants_db
  jpa:
    hibernate:
      ddl-auto: update
eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka
